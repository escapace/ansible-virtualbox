---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
lint: |
    yamllint .
    ansible-lint .
platforms:
  - name: instance
    box: centos/8
provisioner:
  name: ansible
  options:
    become: true
